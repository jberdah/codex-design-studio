# Optional: override the current Codex model used by App Server.
CODEX_STUDIO_MODEL=gpt-5.6-sol

# Optional: force deterministic local refinements for offline demos or E2E tests.
# NEXT_PUBLIC_CODEX_STUDIO_MODE=fallback
